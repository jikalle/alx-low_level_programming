#include <stdio.h>
/**
 * this is the main-entry point
 *
 * Return:0 if no errors, return non-zero if error
 */
int main(void)
{
	printf("With proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
